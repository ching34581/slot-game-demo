# macOS / system
.DS_Store
Thumbs.db

# large binaries not tracked in repo
*.mp4

# manual version snapshots (use git tags instead)
Fortune X Hunter v*.html

# playwright / local tooling artifacts
.playwright-cli/
